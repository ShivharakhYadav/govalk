# Output of `go list -m all` — regenerated as of P8 (sharded store core).
# Only the main module should ever appear here. Any additional line means
# a dependency was introduced and the zero-dependency rule is broken.
github.com/ShivharakhYadav/govalk
